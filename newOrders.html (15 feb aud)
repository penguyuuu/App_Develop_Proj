{% extends "base.html" %}
{% block title %}SweetTalk - Order BubbleTea{% endblock %}

{% block content %}
{% from "includes/_formHelper.html" import render_field %}

<h1 class="display-4">Order Bubble Tea</h1>

<!-- Display selected bubble tea image -->
<div class="text-center">
    <img src="{{ url_for('static', filename=request.args.get('image', 'default.png')) }}" 
         alt="Selected Bubble Tea" class="img-fluid rounded">
</div>

<form method="POST" action="{{ url_for('add_to_cart', image=request.args.get('image')) }}"> 
    <div class="form-group">
        {{ render_field(form.size, class="form-control") }}
    </div>
    <div class="form-group">
        {{ render_field(form.sweetness, class="form-control") }}
    </div>
    <div class="form-group">
        {{ render_field(form.temperature, class="form-control") }}
    </div>
    <div id="prof">
        <div class="form-group">
            {{ render_field(form.remarks, class="form-control") }}
        </div>
    </div>
    <input type="submit" value="Add to Cart" class="btn btn-primary"/>
</form>

<br><br>
<a href="{{ url_for('orders') }}" class="btn btn-secondary">Back to Menu</a>

{% endblock %}
